projection implementation in class zbp_c_hmc_rap_product unique;
strict ( 2 );
extensible;
use draft;
use side effects;

define behavior for ZC_HMC_RAP_PRODUCT alias ZrHmcRapProduct
extensible
use etag
{
  use create;
  use update;
  use delete;

  use action Activate;
  use action Resume;
  use action Discard;
  use action Edit;
  use action Prepare;
}